pipeline{
     agent any
     tools{
         maven 'Maven'
         jdk 'jdk'

          }
   stages {
       stage('Git checkout')
       {
            steps{
                 git 'https://github.com/sravanin15/SpringCricket.git'
            }
       }
      stage("validate"){
          steps{
              sh 'mvn -f codingChallenge -B -DskipTests clean validate'
          }
      }
       stage("compile"){
          steps{
              sh 'mvn  -f codingChallenge compile'
          }
      }
      stage("package"){
          steps{
               sh 'mvn -f codingChallenge  -B -DskipTests package '
          }
      }
   }
}
